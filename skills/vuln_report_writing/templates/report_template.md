# 漏洞报告模板（填写版）

> 提交前删除本行与全部〔图〕说明后替换为真实截图。文中〔图n〕为图位占位。

## 【标题】[系统名]存在[漏洞类]，[攻击者可…/最大危害]

**一、漏洞摘要【必填】**
[系统/平台] 的 [端点/模块] 存在 [漏洞类]，攻击者可 [未授权读取/绕过校验/写入…]，导致 [敏感数据类型 + 规模] 泄露/[业务后果]。仅凭 [xxx 参数/请求头] 即可 [危害动作]。

**二、受影响资产【必填】**
- `[受影响 URL/域名/接口路径]`

**三、复现手册【必填】**

1. 信息收集：对 [目标] 资产进行信息收集，发现 [站点/域名]
   〔图1〕此处放图：[站点首页/登录页截图]
2. JS/配置分析：访问 [URL]，从 [JS文件URL] 中发现：
   - 网关前缀/真实后端：`[xxx]`
   - AppId/请求头：`[xxx]`
   - 接口契约：`[路由?参数]`
   〔图2〕此处放图：JS 中定位到 [关键常量/网关/AppId] 的代码段
3. 访问 [接口URL]，提示 [401/403/权限失败]（说明默认有校验）
   〔图3〕此处放图：被拦截/报错截图
4. 绕过方式：[一句话讲清根因，如"仅校验 Authorization 非空 / 固定头 from:Y / 白名单包含判断"]，构造请求如下：
   ```http
   [完整 raw 数据包，方法/路径/Host/关键头/空行/body]
   ```
   注：[签名/时间戳] 用下方命令计算：输出第1行=xx、第2行=xx
   ```bash
   python -X utf8 -c "…"
   ```
5. 请求成功，响应返回 [关键敏感字段]
   〔图4〕此处放图：Burp 响应/页面，可见 [xxx 字段]
6. 危害放大：参数 [userId/工号/orgId] 可遍历/全量枚举，可获取 [全部 xx]
   〔图5〕此处放图：遍历/批量/全量数据列表截图
   〔图6〕此处放图：危害落地物（下载的文档/合同/订单等）
7. 合规说明：本次仅做 [只读/最小] 证明，[写操作/进一步利用] 未实际执行（理论上可 [危害]），相关数据已 [还原/脱敏]。

**四、附件 POC**（如有脚本/录屏，打包 zip 并附 README 说明步骤对应关系）

**五、风险影响评估【必填】**
1. 机密性：[哪些数据、全量?、可遍历?、实时?]
2. 完整性：[能否写/篡改/伪造 → 业务后果]
3. 可用性：[中断/钓鱼/接管 → 业务后果]

**六、修复建议【选填】**
- [原理性修复：完善 XX 接口权限校验 / 认证改私钥签名+服务端验签 / 按身份校验订阅归属 / 移除 noLogin 写接口]
